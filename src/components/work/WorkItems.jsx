/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */
import React from 'react'

const WorkItems = ({ item }) => {
  return <div className="work__card">{item}</div>;
};

export default WorkItems